#include <stdlib.h>
#include <stdio.h>

/**
 * mod - the mod of two integers together
 * @a: this is the first unit
 * @b: this is the second unit
 * Return: the answer
 */
int add(int a, int b)
{       
        return (a % b);
}  
