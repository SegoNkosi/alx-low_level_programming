#include <stdio.h>
/**
 * * main - function for alphabets in lower and uppercase
 * Return: always 0
 */
int main(void)
{
	char j;

	for (j = 'a'; j <= 'z'; j++)
	putchar(j);
	putchar('\n');
	return (0);
}
