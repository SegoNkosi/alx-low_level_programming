#include <stdio.h>
/**
 * main - function that prints uppercase and lowercase alphabets
 * Return: always 0
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	putchar(i);

	for (I = 'A'; I <= 'Z'; I++)
	putchar(I);
	putchar('\n');
	return (0);
}
